$(document).on('turbolinks:load', function() {
  attachListeners();
})

var showUser = (id) => {
  // shows a user's profile
  //includes next button that takes to another user's page
  // $.get(`/users/${id}`, function(data) {
  //
  // }
}


function attachListeners() {
  $('.user-show').on('click', function() {
    debugger
  })
}
